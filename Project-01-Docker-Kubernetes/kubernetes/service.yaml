apiVersion: v1
kind: Service

metadata:
  name: hverse-service

spec:
  type: NodePort

  selector:
    app: hverse-web

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
